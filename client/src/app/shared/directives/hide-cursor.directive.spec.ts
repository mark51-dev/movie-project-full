import { HideCursorDirective } from './hide-cursor.directive';

describe('HideCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new HideCursorDirective();
    expect(directive).toBeTruthy();
  });
});

import { HidePlayerControlsDirective } from './hide-player-controls.directive';

describe('HidePlayerControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new HidePlayerControlsDirective();
    expect(directive).toBeTruthy();
  });
});
